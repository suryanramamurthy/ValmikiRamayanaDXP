"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/lib/content-type.ts
var content_type_exports = {};
__export(content_type_exports, {
  ContentType: () => ContentType
});
module.exports = __toCommonJS(content_type_exports);
var import_core = require("@contentstack/core");
var import_entry = require("./entry.cjs");
var import_entries = require("./entries.cjs");
var ContentType = class {
  constructor(client, contentTypeUid) {
    this._queryParams = {};
    this._client = client;
    this._contentTypeUid = contentTypeUid;
    this._urlPath = `/content_types/${this._contentTypeUid}`;
  }
  entry(uid) {
    if (uid) return new import_entry.Entry(this._client, this._contentTypeUid, uid);
    return new import_entries.Entries(this._client, this._contentTypeUid);
  }
  /**
   * @method fetch
   * @memberof ContentType
   * @description Fetches the contentType data on the basis of the contentType uid
   * @returns {Promise<T>} Promise that resolves to the content type data
   * @example
   * import contentstack from '@contentstack/delivery-sdk'
   *
   * const stack = contentstack.stack({ apiKey: "apiKey", deliveryToken: "deliveryToken", environment: "environment" });
   * const result = await stack.contentType(contenttype_uid).fetch();
   */
  async fetch() {
    const response = await (0, import_core.getData)(this._client, this._urlPath);
    if (response.content_type) return response.content_type;
    return response;
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  ContentType
});
//# sourceMappingURL=content-type.cjs.map