import { Region, params } from './types.cjs';
import '@contentstack/core';
import '../persistance/types/storage-type.cjs';
import '../persistance/config/persistance-storage-config.cjs';
import '../persistance/types/storage.cjs';
import '../persistance/helper/utils.cjs';

declare function getHostforRegion(region: Region | string, host?: string): string;
/**
 * Checks if the code is running in a browser environment
 * @returns {boolean} True if running in browser, false otherwise
 */
declare function isBrowser(): boolean;
/**
 * Encodes query parameters recursively, handling nested objects
 * @param {params} params - Query parameters object to encode
 * @returns {params} Encoded query parameters object
 */
declare function encodeQueryParams(params: params): params;

export { encodeQueryParams, getHostforRegion, isBrowser };
