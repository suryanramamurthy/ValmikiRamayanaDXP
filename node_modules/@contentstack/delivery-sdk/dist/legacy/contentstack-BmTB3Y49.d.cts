import { Stack } from './lib/stack.cjs';
import { StackConfig } from './lib/types.cjs';
import * as utils from '@contentstack/utils';

/**
 * @method stack
 * @memberof Contentstack
 * @description Creates a stack instance
 * @param {StackConfig} config - config object for stack with apiKey, deliveryToken and environment as required fields
 * @returns {StackClass} Stack instance
 * @example
 * import contentstack from '@contentstack/delivery-sdk'
 * const stack = contentstack.stack({ apiKey: "apiKey", deliveryToken: "deliveryToken", environment: "environment" });
 * @example
 * import contentstack from '@contentstack/delivery-sdk'
 * const stack = contentstack.stack({
 *   apiKey: "apiKey",
 *   deliveryToken: "deliveryToken",
 *   environment: "environment",
 *   region:"region",
 *   locale:"locale",
 *   cacheOptions: {
 *    policy: Policy.CACHE_THEN_NETWORK,
 *    storeType: 'localStorage'
 *   }
 * });
 */
declare function stack(config: StackConfig): Stack;

declare const contentstack_stack: typeof stack;
declare namespace contentstack {
  export { utils as Utils, contentstack_stack as stack };
}

export { contentstack as c, stack as s };
