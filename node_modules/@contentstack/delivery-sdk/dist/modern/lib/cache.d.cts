import { CacheOptions } from './types.cjs';
import '@contentstack/core';
import '../persistance/types/storage-type.cjs';
import '../persistance/config/persistance-storage-config.cjs';
import '../persistance/types/storage.cjs';
import '../persistance/helper/utils.cjs';

/**
 * Handles cache requests based on the cache policy
 * @param {CacheOptions} cacheOptions - Cache configuration options
 * @param {string} apiKey - API key for cache key generation
 * @param {Function} defaultAdapter - Default axios adapter function
 * @param {Function} resolve - Promise resolve function
 * @param {Function} reject - Promise reject function
 * @param {object} config - Request configuration object
 * @returns {Promise} Resolves or rejects based on cache policy and API response
 */
declare function handleRequest(cacheOptions: CacheOptions, apiKey: string, defaultAdapter: any, resolve: any, reject: any, config: any): Promise<any>;

export { handleRequest };
