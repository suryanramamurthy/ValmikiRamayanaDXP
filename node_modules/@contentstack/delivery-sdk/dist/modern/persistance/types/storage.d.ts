import { Callback } from '../helper/utils.js';

interface Storage {
    name: string;
    getItem: (key: string) => string | null;
    setItem: (key: string, value: string) => void;
    removeItem: (key: string) => void;
    each: (callback: Callback) => void;
    clear: () => void;
}

export type { Storage };
